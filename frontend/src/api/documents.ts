import { api } from './index'

export const listDocuments = api.documents.list
export const uploadDocument = api.documents.upload
export const deleteDocument = api.documents.remove
export const downloadDocument = api.documents.download

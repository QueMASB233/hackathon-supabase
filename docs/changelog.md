# Changelog

## [0.1.0]

### Added

- Estructura de repo: `frontend/`, `backend/`, `supabase/`, `docs/`.
- PWA shell (manifest, service worker, iconos, install, aviso de update).
- Design system (tokens carbon/chamber/seal, componentes, boot).
- Auth passwordless: correo + código 2FA de 6 dígitos, invitación, estados de código.
- Business dashboard y alta de cliente (vía API adapter).
- Workspace chat-first, conversaciones, documentos, actividad gated.
- Optimistic UI en chat y conversaciones.
- Adapters `mock` y `http`. Mocks aislados.
- Loading (boot + skeletons), empty/error, offline awareness.
- Documentación inicial.

### Changed

- Nada (versión inicial).

### Fixed

- Nada (versión inicial).
